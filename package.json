{
  "name": "phaser-ai-fantasy-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Universal Phaser + TypeScript starter for AI-assisted fantasy game prototypes.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:atlas": "node tools/build-grid-atlas.mjs",
    "validate:assets": "node tools/validate-assets.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
